services:
  caddy:
    image: caddy:2.10-alpine
    restart: always
    environment:
      ALGOSPHERE_DOMAIN: ${ALGOSPHERE_DOMAIN:?Set ALGOSPHERE_DOMAIN}
    ports:
      - "80:80"
      - "443:443"
      - "443:443/udp"
    volumes:
      - ./infra/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy_data:/data
      - caddy_config:/config
    networks:
      - backend
    depends_on:
      frontend:
        condition: service_healthy

volumes:
  caddy_data:
  caddy_config:
